.env
SophiaAI-venv/

# .remember/ is tracked, except its transient scratch files
.remember/
__pycache__/
*.pyc

# Note: data/embeddings.npy and data/sophia_index.faiss (~2MB each) are
# committed so a fresh clone runs without rebuilding the FAISS index.

# Runtime database — created on app boot, not tracked
*.db
sophia_memory.db

# graphify knowledge graph — regenerable from the repo, not tracked
graphify-out/