/**
 * Mental Model:
 *   A quiet footer line for the public pages — Sophia's reason for being,
 *   stated once, in dim text beneath the content.
 */
export default function SiteFooter() {
  return (
    <footer className="site-footer">
      SophiaAI — a bridge between the Divine and Technology.
    </footer>
  );
}
